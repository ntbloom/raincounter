#include <cstddef>

/* how much does the gauge measure in inches */
constexpr float GAUGE_STD = 0.11;

/* how much does the gauge measure in millimeters */
constexpr float GAUGE_MET = 0.2794;

/* how frequently to take a measurement, in seconds */
constexpr size_t TEMP_INTERVAL = 3;
